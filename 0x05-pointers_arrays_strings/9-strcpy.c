#include "main.h"

/**
 *   _strcpy - Copy the string
 *   @dest: The character to find
 *   @src: Source value to check
 *   Return: 0 (success)
 */

char *_strcpy(char *dest, char *src)
{
int x;
for (x = 0; src[x] != '\0'; x++)
{
dest[i] = src[x];
}
dest[x++] = '\0';
return (dest);
}
